#!/usr/bin/env bash
# direnv configuration for automatic Nix environment loading

# Load the Nix flake development shell
use flake

# Show a helpful message when the environment loads
echo "🚀 Jekyll development environment activated!"
echo "Run 'just --list' to see available commands"